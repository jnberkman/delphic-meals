[build]
builder = "nixpacks"

[deploy]
startCommand = "npx knex migrate:latest && node src/index.js"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
